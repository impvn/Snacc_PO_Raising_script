{
  "name": "snacc-po-raiser-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that raises bulk Purchase Orders on SupplyNote from a shared Google Sheets forecast.",
  "scripts": {
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "test:parity": "python3 ../tools/parity_check.py"
  }
}
